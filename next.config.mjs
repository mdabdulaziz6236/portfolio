/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images:{
    domains: ['drive.google.com',"i.ibb.co.com,'via.placeholder.com"]
  }
};

export default nextConfig;
